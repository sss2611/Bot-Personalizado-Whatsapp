const express = require('express');
const router = express.Router();

// ese archivo no es necesario para el funcionamiento del bot con Baileys, a menos que estés integrando una API externa o querés exponer endpoints para enviar mensajes manualmente
module.exports = router;
